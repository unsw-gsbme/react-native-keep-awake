describe('Keep Awake', () => {
  it.todo('On mount it should call activate');
  it.todo('On unmount it should call deactivate');
  it.todo(
    'Mounting multiple KeepAwake should call activate on the first mount'
  );
  it.todo('Mounting multiple KeepAwake should increase reference count');
  it.todo(
    'On unmount a Keep Awake it should only call deactivate on last unmounted component'
  );
});

describe('useKeepAwake', () => {
  it.todo('On mount it should call activate');
  it.todo('On unmount it should call deactivate');
});

describe('global commands', () => {
  describe('activate', () => {
    it.todo('Calling activate should activate screen awake');
  });

  describe('deactivate', () => {
    it.todo('Calling deactivate should activate screen awake');
  });
});
